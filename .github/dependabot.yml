version: 2

updates:
  - package-ecosystem: composer
    directory: "/"
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
      timezone: America/New_York
    open-pull-requests-limit: 10
    labels: [dependencies]
    groups:
      # The toolchain moves together and is verified by the same CI run, so one PR per week
      # rather than five. A failure in any of them means the same thing: fix or hold.
      dev-toolchain:
        dependency-type: development
        patterns:
          - "phpstan/*"
          - "larastan/*"
          - "laravel/pint"
          - "rector/*"
          - "pestphp/*"
          - "phpunit/*"
          - "mockery/*"
          - "orchestra/*"
      # The optional integrations this package adapts to. Grouped because a break in one is
      # an adapter problem, not a dependency problem, and they are reviewed the same way.
      optional-integrations:
        dependency-type: development
        patterns:
          - "laravel/sanctum"
          - "laravel/passport"
          - "tymon/jwt-auth"
          - "stancl/tenancy"

  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
      day: monday
      time: "06:00"
      timezone: America/New_York
    open-pull-requests-limit: 5
    labels: [dependencies, ci]
    groups:
      # Actions are SHA-pinned with a trailing version comment; Dependabot rewrites both.
      # Re-pin on every merged PR rather than letting a tag drift — a moved tag is how the
      # tj-actions compromise spread.
      actions:
        patterns:
          - "*"
